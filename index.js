/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import DocumentScannerPrototype from "./App";

AppRegistry.registerComponent(appName, () => DocumentScannerPrototype);
